import 'package:flutter/material.dart';

class NonadniPage extends StatelessWidget {
  const NonadniPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("📝 Nonadni Page", style: TextStyle(fontSize: 24)),
    );
  }
}